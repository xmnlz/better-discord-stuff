{
    "name": "better-discord-stuff",
    "description": "All staff for BetterDiscord.",
    "private": true,
    "devDependencies": {
        "bundlebd": "^1.0.0"
    },
    "scripts": {
        "build": "npx bundlebd",
        "dev": "npx bundlebd --development",
        "vus:build": "yarn build UserVoiceShow",
        "vus:dev": "yarn dev UserVoiceShow"
    },
    "dependencies": {
        "@types/react": "^18.0.17",
        "react": "^18.2.0"
    }
}
